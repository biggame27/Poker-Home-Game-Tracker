"use client"

import {
  CircleCheckIcon,
  InfoIcon,
  Loader2Icon,
  OctagonXIcon,
  TriangleAlertIcon,
} from "lucide-react"
// Placeholder Toaster that renders nothing while the external package is unavailable.
export type ToasterProps = Record<string, unknown>

const Toaster = (_props: ToasterProps) => null

export { Toaster }
